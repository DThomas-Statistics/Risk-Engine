def Single_day_monte_carlo(Volatility,alpha,s,df):
  #need to add a df formula
  simulation = np.array(np.random.standard_t(df,s))
  Simulated_r= simulation*Volatility
  q=np.sort(Simulated_r)
  VaR = q[round(alpha*s)]
  ABS_VaR = np.abs(VaR)
  return ABS_VaR
